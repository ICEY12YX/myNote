___
https://gitbookcpp.llfc.club/sections/cpp/concurrent/concpp17.html

# 实现

先定义链表的基本结构
```cpp
template<typename T>
class threadsafe_list
{
    //基本的链表节点
    struct node
    {
        std::mutex m;  //每个节点独有一个锁!!!
        std::shared_ptr<T> data;  //每个节点的数据
        std::unique_ptr<node> next;

        //默认构造函数
        //创建一个 空节点（data 未初始化，next 置空）
        //next是unique_ptr类型  std::unique_ptr 默认是 nullptr
        //所以相当于next(nullptr)
        node() : next(){}

        //有value传进来的构造
        node(T const& value) : data(std::make_shared<T>(value)){}
    };

    node head;  //头结点(里面不存实际的数据,里面的data是没用的)
public:
    threadsafe_list(){}

    ~threadsafe_list(){}

    threadsafe_list(threadsafe_list const& other) = delete;
    threadsafe_list& operator=(threadsafe_list const& other) = delete;
}
```

我们先实现一个析构函数
我们期望析构函数能够从头到尾的删除元素
实现析构之前,先实现一个删除函数
```cpp
template<typename Predicate>
void remove_if(Predicate p)//把p当做是个谓词  或者就当做lambda
{
    //取到头结点的指针
    node* current = &head;
    //给头结点上锁,(看下面代码,lk应该是指current节点的锁)
    std::unique_lock<std::mutex> lk(head.m);

    //循环一直往头结点后面取
    //要是下一个节点为空,就退出循环
    while (node* const next = current->next.get())
    {
        //先给next节点上锁
        std::unique_lock<std::mutex> next_lk(next->m);

        //判断 next->data 是否满足 p
        //p是[](node const&) {return true; }
        if (p(*next->data))
        {
            //条件满足,就把next先存起来
            //(因为上面取的next是node*类型,所以不能直接赋值给old_next)
            std::unique_ptr<node> old_next = std::move(current->next);
            //让 current 跳过 next，直接指向 next->next
            //就是current指向先不变,然后把后面的一个个删掉
            current->next = std::move(next->next);
            next_lk.unlock(); //解锁

            //old_next 由 unique_ptr 管理，离开作用域时会自动调用 ~node()。!!!
        }
        else
        {
            //条件不满足的时候
            lk.unlock();  //current结点先解锁
            current = next;  //current往后
            lk = std::move(next_lk);  //current往后了,那它的锁也要跟上
            //(因为条件不满足,当前结点就不删,往后找满足条件的节点 去删掉)
        }
    }
}
```
解释
    ![[Pasted image 20250718193315.png]]
    (* next->data) 就是传值到lambda里面
    我们p里面都是return true 意思就是,无条件全部删除

析构
```cpp
~threadsafe_list()
{
    //就是全部结点都符合条件
    //全删
    remove_if([](node const&) {return true; });
}
```


头插
```cpp
void push_front(T const& value)
{
    //创建新结点,存入新值value
    std::unique_ptr<node> new_node(new node(value));
    //头结点上锁(其他线程不能来操作头)
    std::lock_guard<std::mutex> lk(head.m);

    //连上
    new_node->next = std::move(head.next);
    head.next = std::move(new_node);
}
```

根据谓词,在链表中查找对应的节点数据
```cpp
    template<typename Predicate>
    std::shared_ptr<T> find_first_if(Predicate p)
    {
        //current代表查找位置
        //从头结点开始
        node* current = &head;
        std::unique_lock<std::mutex> lk(head.m);  //current节点对应的锁

        //不断向后找
        //current后一个为空,就离开循环
        while (node* const next = current->next.get())
        {
            //给next加锁
            std::unique_lock<std::mutex> next_lk(next->m);
            //current解锁(因为后面我们要读的只是next节点,不需要读current)
            //就算current被删了也没事,我们后面让current = next,无关current是否悬空
            lk.unlock(); 
            if (p(*next->data))
            {
                //条件满足就返回对应的数据
                //然后就退出循环了
                return next->data;
            }
            //条件不满足,就往后找
            current = next;
            lk = std::move(next_lk);
        }
        //啥也找不到,就返回空的shared_ptr
        return std::shared_ptr<T>();
    }
```

遍历所有节点
```cpp
template<typename Function>
void for_each(Function f)
{
    //current代表查找位置
    //从头结点开始
    node* current = &head;
    std::unique_lock<std::mutex> lk(head.m);
    while (node* const next = current->next.get())
    {
        std::unique_lock<std::mutex> next_lk(next->m);
        lk.unlock();
        
        f(*next->data);  //对每个结点的数据执行f函数

        //往后
        current = next;
        lk = std::move(next_lk);
    }
}
```

尾插
因为有可能头插,尾插是往同一个地方插
所以为了实现尾插 我们给链表加两个成员
`node_d* last_node_ptr;`  //尾结点指针

`std::mutex last_ptr_mtx;` //一把锁, 看下面解释
```cpp
void push_back(T const& value) {
    //先创建新结点
    std::unique_ptr<node_d> new_node(new node_d(value));

    //尾结点锁
    std::unique_lock<std::mutex> lk(last_node_ptr->m);
    //看下面解释
    std::unique_lock<std::mutex> last_lk(last_ptr_mtx);
    
    //原来的最后节点的下一个节点指向新生成的节点
    last_node_ptr->next = std::move(new_node);
    //将最后一个节点后移
    last_node_ptr = last_node_ptr->next.get();
}
```
解释
    ![[Pasted image 20250718201327.png]]


头插也要更改
```cpp
void push_front(T const& value)
{
    std::unique_ptr<node_d> new_node(new node_d(value));
    //头结点上锁
    std::lock_guard<std::mutex> lk(head.m);

    //连上
    new_node->next = std::move(head.next);
    head.next = std::move(new_node);

    //当头结点下一个的下一个,就是上面连上以后,新节点的下一个就是空指针
    //说明尾结点要改了
    if (head.next->next == nullptr) {
        //尾结点的修改锁 锁上
        std::lock_guard<std::mutex> last_lk(last_ptr_mtx);
        //修改尾结点指针
        last_node_ptr = head.next.get();
    }
}
```
解释
    ![[Pasted image 20250718202320.png]]

其他的修改(删除操作)
```cpp
template<typename Predicate>
void remove_if(Predicate p)
{
    node_d* current = &head;
    std::unique_lock<std::mutex> lk(head.m);
    
    while (node_d* const next = current->next.get())
    {
        std::unique_lock<std::mutex> next_lk(next->m);
        if (p(*next->data))
        {
            std::unique_ptr<node_d> old_next = std::move(current->next);
            current->next = std::move(next->next);

            //多了这部分
            //判断上面删除了以后是否current就是最后一个节点
            if (current->next == nullptr) {
                std::lock_guard<std::mutex> last_lk(last_ptr_mtx);
                last_node_ptr = current;  
            }
            next_lk.unlock();
        }
        else
        {
            lk.unlock();
            current = next;
            lk = std::move(next_lk);
        }
    }
}

template<typename Predicate>
bool remove_first(Predicate p)  //只找第一个符合条件的
{
    node_d* current = &head;
    std::unique_lock<std::mutex> lk(head.m);
    
    while (node_d* const next = current->next.get())
    {
        std::unique_lock<std::mutex> next_lk(next->m);
        if (p(*next->data))
        {
            std::unique_ptr<node_d> old_next = std::move(current->next);
            current->next = std::move(next->next);
            
            //多了这部分
            //判断上面删除了以后是否current就是最后一个节点
            if (current->next == nullptr) {
                std::lock_guard<std::mutex> last_lk(last_ptr_mtx);
                last_node_ptr = current;
            }
            next_lk.unlock();

            return true;
        }

        lk.unlock();
        current = next;
        lk = std::move(next_lk);
    }

    return false;
}
```


思考删除操作和push_back是否会产生线程竞争，答案是会的
(但是这个我的理解是不会出问题,但是大佬觉得会出问题,deepseek的回答稍微有点怪)
    ![[Pasted image 20250724152446.png]]
    ![[Pasted image 20250724152458.png]]

为什么会"`last_node_ptr` 已经被改为 `B`，但线程 2 仍然持有 `C.m`？"
有点怪
    ![[Pasted image 20250724153106.png]]

修改push_back
先锁尾节点修改锁,再锁尾结点锁
```cpp
void push_back(T const& value) {
    std::unique_ptr<node_d> new_node(new node_d(value));
    std::unique_lock<std::mutex> last_lk(last_ptr_mtx);
    std::unique_lock<std::mutex> lk(last_node_ptr->m);
    //原来的最后节点的下一个节点指向新生成的节点
    last_node_ptr->next = std::move(new_node);
    //将最后一个节点后移
    last_node_ptr = last_node_ptr->next.get();
}
```
解释
    ![[Pasted image 20250724154433.png]]
但是问题可以解决的前提是
线程1先执行`remove_if`,先执行了`std::unique_lock<std::mutex> lk(head.m);`锁上了B
执行` std::unique_lock<std::mutex> next_lk(next->m);` 锁上了C
然后后面要修改尾结点时执行`std::lock_guard<std::mutex> last_lk(last_ptr_mtx);`
此时线程2才来执行

要是线程2刚开始就执行就会先执行`std::unique_lock<std::mutex> last_lk(last_ptr_mtx);`
然后C的锁拿不到,所以阻塞在那里
但是此时线程1执行到`std::lock_guard<std::mutex> last_lk(last_ptr_mtx);`,也阻塞起来
就死锁了

所以要再修改
```cpp
void push_back(T const& value) {
    std::unique_ptr<node_d> new_node(new node_d(value));

    //要锁一起锁
    std::lock(last_node_ptr->m, last_ptr_mtx);
    std::unique_lock<std::mutex> lk(last_node_ptr->m, std::adopt_lock);
    std::unique_lock<std::mutex> last_lk(last_ptr_mtx, std::adopt_lock);
    //原来的最后节点的下一个节点指向新生成的节点
    last_node_ptr->next = std::move(new_node);
    //将最后一个节点后移
    last_node_ptr = last_node_ptr->next.get();
}
```
___
# 完整代码

不实现尾插的链表
```cpp
﻿#pragma once
#include <mutex>
#include <memory>

template<typename T>
class threadsafe_list
{
	struct node
	{
		std::mutex m;
		std::shared_ptr<T> data;
		std::unique_ptr<node> next;
		node() : next(){}
		node(T const& value) : data(std::make_shared<T>(value)){}
	};

	node head;
public:
	threadsafe_list(){}

	~threadsafe_list()
	{
		remove_if([](node const&) {return true; });
	}

	threadsafe_list(threadsafe_list const& other) = delete;
	threadsafe_list& operator=(threadsafe_list const& other) = delete;

	void push_front(T const& value)
	{
		std::unique_ptr<node> new_node(new node(value));
		std::lock_guard<std::mutex> lk(head.m);
		new_node->next = std::move(head.next);
		head.next = std::move(new_node);
	}

	template<typename Function>
	void for_each(Function f)
	{
		node* current = &head;
		std::unique_lock<std::mutex> lk(head.m);
		while (node* const next = current->next.get())
		{
			std::unique_lock<std::mutex> next_lk(next->m);
			lk.unlock();
			f(*next->data);
			current = next;
			lk = std::move(next_lk);
		}
	}

	template<typename Predicate>
	std::shared_ptr<T> find_first_if(Predicate p)
	{
		node* current = &head;
		std::unique_lock<std::mutex> lk(head.m);
		while (node* const next = current->next.get())
		{
			std::unique_lock<std::mutex> next_lk(next->m);
			lk.unlock();
			if (p(*next->data))
			{
				return next->data;
			}
			current = next;
			lk = std::move(next_lk);
		}
		return std::shared_ptr<T>();
	}

	template<typename Predicate>
	void remove_if(Predicate p)
	{
		node* current = &head;
		std::unique_lock<std::mutex> lk(head.m);
		while (node* const next = current->next.get())
		{
			std::unique_lock<std::mutex> next_lk(next->m);
			if (p(*next->data))
			{
				std::unique_ptr<node> old_next = std::move(current->next);
				current->next = std::move(next->next);
				next_lk.unlock();
			}
			else
			{
				lk.unlock();
				current = next;
				lk = std::move(next_lk);
			}
		}
	}
};
```

实现尾插的链表
```cpp
#pragma once
#include <mutex>
#include <memory>

template<typename T>
class double_push_list
{
	struct node_d
	{
		std::mutex m;
		std::shared_ptr<T> data;
		std::unique_ptr<node_d> next;
		node_d() :
			next()
		{}
		node_d(T const& value) :
			data(std::make_shared<T>(value))
		{}
	};

	node_d head;
	node_d* last_node_ptr;
	//控制最后一个节点指针更新
	std::mutex last_ptr_mtx;
public:
	double_push_list()
	{
		last_node_ptr = &head;
	}

	~double_push_list()
	{
		remove_if([](node_d const&) {return true; });
	}

	double_push_list(double_push_list const& other) = delete;
	double_push_list& operator=(double_push_list const& other) = delete;

	void push_front(T const& value)
	{
		std::unique_ptr<node_d> new_node(new node_d(value));
		std::lock_guard<std::mutex> lk(head.m);
		new_node->next = std::move(head.next);
		head.next = std::move(new_node);
		//更新最后一个节点
		if (head.next->next == nullptr) {
			std::lock_guard<std::mutex> last_lk(last_ptr_mtx);
			last_node_ptr = head.next.get();
		}
	}

	void push_back(T const& value) {
		//防止于push_head同时进行
		//并且保证头部或者删除节点更新last_node_ptr唯一, 所以同时加锁
		std::unique_ptr<node_d> new_node(new node_d(value));
		std::lock(last_node_ptr->m, last_ptr_mtx);
		std::unique_lock<std::mutex> lk(last_node_ptr->m, std::adopt_lock);
		std::unique_lock<std::mutex> last_lk(last_ptr_mtx, std::adopt_lock);
		//原来的最后节点的下一个节点指向新生成的节点
		last_node_ptr->next = std::move(new_node);
		//将最后一个节点后移
		last_node_ptr = last_node_ptr->next.get();
	}

	template<typename Function>
	void for_each(Function f)
	{
		node_d* current = &head;
		std::unique_lock<std::mutex> lk(head.m);
		while (node_d* const next = current->next.get())
		{
			std::unique_lock<std::mutex> next_lk(next->m);
			lk.unlock();
			f(*next->data);
			current = next;
			lk = std::move(next_lk);
		}
	}

	template<typename Predicate>
	std::shared_ptr<T> find_first_if(Predicate p)
	{
		node_d* current = &head;
		std::unique_lock<std::mutex> lk(head.m);
		while (node_d* const next = current->next.get())
		{
			std::unique_lock<std::mutex> next_lk(next->m);
			lk.unlock();
			if (p(*next->data))
			{
				return next->data;
			}
			current = next;
			lk = std::move(next_lk);
		}
		return std::shared_ptr<T>();
	}

	template<typename Predicate>
	void remove_if(Predicate p)
	{
		node_d* current = &head;
		std::unique_lock<std::mutex> lk(head.m);
		while (node_d* const next = current->next.get())
		{
			std::unique_lock<std::mutex> next_lk(next->m);
			if (p(*next->data))
			{
				std::unique_ptr<node_d> old_next = std::move(current->next);
				current->next = std::move(next->next);
				//判断删除的是否为最后一个节点
				if (current->next == nullptr) {
					std::lock_guard<std::mutex> last_lk(last_ptr_mtx);
					last_node_ptr = current;
				}
				next_lk.unlock();
			}
			else
			{
				lk.unlock();
				current = next;
				lk = std::move(next_lk);
			}
		}
	}

	template<typename Predicate>
	bool remove_first(Predicate p)
	{
		node_d* current = &head;
		std::unique_lock<std::mutex> lk(head.m);
		while (node_d* const next = current->next.get())
		{
			std::unique_lock<std::mutex> next_lk(next->m);
			if (p(*next->data))
			{
				std::unique_ptr<node_d> old_next = std::move(current->next);
				current->next = std::move(next->next);
				//判断删除的是否为最后一个节点
				if (current->next == nullptr) {
					std::lock_guard<std::mutex> last_lk(last_ptr_mtx);
					last_node_ptr = current;
				}
				next_lk.unlock();

				return true;
			}

			lk.unlock();
			current = next;
			lk = std::move(next_lk);
		}

		return false;
	}

	//从断言处查询满足条件的第一个点之前插入
	template<typename Predicate>
	void insert_if(Predicate p, T const & value)
	{
		node_d* current = &head;
		std::unique_lock<std::mutex> lk(head.m);
		while(node_d * const next = current->next.get())
		{
			std::unique_lock<std::mutex> next_lk(next->m);
			if(p(*(next->data)))
			{
				std::unique_ptr<node_d> new_node(new node_d(value));

				//缓存当前节点的下一个节点作为旧节点
				auto old_next = std::move(current->next);

				//新节点指向当前节点的下一个节点
				new_node->next = std::move(old_next);
				//当前节点的下一个节点更新为新节点
				current->next = std::move(new_node);
				return;
			}
			lk.unlock();
			current = next;
			lk = std::move(next_lk);
		}
	}
};
```

调用
```cpp
#pragma once
#include <iostream>

class MyClass
{
public:
	MyClass(int i) :_data(i) {}

	friend std::ostream& operator << (std::ostream& os, const MyClass& mc) {
		os << mc._data;
		return os;
	}

	int GetData() const
	{
		return _data;
	}

private:
	int _data;
};
```

```cpp
﻿// day16-threadsafelist.cpp : 此文件包含 "main" 函数。程序执行将在此处开始并结束。

#include <iostream>
#include "threadsafelist.h"
#include "tailpushlist.h"
#include "MyClass.h"
#include <set>
#include "tailpushlist.h"

std::set<int> removeSet;
void TestThreadSafeList()
{
	
	threadsafe_list<MyClass> thread_safe_list;

    //测试头插
	std::thread t1([&]()
	{
		for(unsigned int i = 0; i < 100; i++)
		{
			MyClass mc(i);
			thread_safe_list.push_front(mc);
		}
		
	});

    //测试查找
	std::thread t2([&]()
	{
		for (unsigned int i = 0; i < 100; )
		{
			
			auto find_res = thread_safe_list.find_first_if([&](auto & mc)
			{
        			//查找0-99的数字 链表里分别都有没有
					return mc.GetData() == i;
			});

            //等于nullptr说明没找到,说明线程1还没来的及插入
            //那就睡一会
			if(find_res == nullptr)
			{
				std::this_thread::sleep_for(std::chrono::milliseconds(10));
				continue;
			}

			removeSet.insert(i);  //不用管
			i++;
		}
	});

	t1.join();
	t2.join();

}


void TestTailPush()
{
	double_push_list<MyClass> thread_safe_list;
	for(int i = 0; i < 10; i++)
	{

		MyClass mc(i);
		thread_safe_list.push_front(mc);
	}

	thread_safe_list.for_each([](const MyClass & mc)
	{
			std::cout << "for each print " << mc << std::endl;
	});


	for(int i = 10; i < 20; i++)
	{
		MyClass mc(i);
		thread_safe_list.push_back(mc);
	}

	thread_safe_list.for_each([](const MyClass& mc)
		{
			std::cout << "for each print " << mc << std::endl;
		});

	auto find_data = thread_safe_list.find_first_if([](const MyClass& mc)
		{
			return (mc.GetData() == 19);
		});

	if(find_data)
	{
		std::cout << "find_data is " << find_data->GetData() << std::endl;
	}


	thread_safe_list.insert_if([](const MyClass& mc)
		{
			return (mc.GetData() == 19);
		},20);


	thread_safe_list.for_each([](const MyClass& mc)
		{
			std::cout << "for each print " << mc << std::endl;
		});
}

void MultiThreadPush()
{
	double_push_list<MyClass> thread_safe_list;

	std::thread t1([&]()
	{
			for (int i = 0; i < 20000; i++)
			{
				MyClass mc(i);
				thread_safe_list.push_front(mc);
				std::cout << "push front " << i << " success" << std::endl;
			}
	});

	std::thread t2([&]()
	{
			for (int i = 20000; i < 40000; i++)
			{
				MyClass mc(i);
				thread_safe_list.push_back(mc);
				std::cout << "push back " << i << " success" << std::endl;
			}
	});

	std::thread t3([&]()
	{
			for(int i = 0; i < 40000; )
			{
				bool rmv_res = thread_safe_list.remove_first([&](const MyClass& mc)
					{
						return mc.GetData() == i;
					});

				if(!rmv_res)
				{
					std::this_thread::sleep_for(std::chrono::milliseconds(10));
					continue;
				}

				i++;
			}
	});

	t1.join();
	t2.join();
	t3.join();

	std::cout << "begin for each print...." << std::endl;
	thread_safe_list.for_each([](const MyClass& mc)
		{
			std::cout << "for each print " << mc << std::endl;
		});
	std::cout << "end for each print...." << std::endl;
}

int main()
{
	//TestThreadSafeList();
	//for(auto & v: removeSet)
	//{
	//	std::cout << "remove data is " << v << std::endl;
	//}

	//TestTailPush();
	MultiThreadPush();

    std::cout << "Hello World!\n";
}



```