___
模板需要编译两次，在第一次编译时仅仅检查最基本的语法，比如括号是否匹配。等函数真正被调用时，才会真正生成需要的类或函数。
所以这直接导致了一个结果，就是<mark style="background: #ABF7F7A6;">不论是模板类还是模板函数，声明与实现都必须放在同一个文件中。因为在程序在编译期就必须知道函数的具体实现过程。如果实现和声明分文件编写，需要在链接时才可以看到函数的具体实现过程，这当然会报错。</mark>
于是人们发明了.hpp文件来存放模板这种声明与实现在同一文件的情况。
    ![[Pasted image 20250613160058.png]]
    ![[Pasted image 20250613160125.png]]
        ![[Pasted image 20250613160144.png]]
    ![[Pasted image 20250613160204.png]]

一个类模版的写法
```cpp
//.hpp文件
template<typename T>
class MyArray {
	using iterator = T*;
	using const_iterator = const T*;

public:
	MyArray(size_t count);
	/*{  //写类里面
		if (count) {
			data = new T[count]();
		}
		else {
			data = nullptr;
		}
	}*/

	~MyArray();

	iterator begin() const;
	const_iterator cbegin() const;

	//其实还要写复制构造函数和赋值运算符(这里没写)
private:
	T* data;
};


//要是函数想在外面定义,就这么写(一般都写在外面)
template<typename T>  //记得先带上模版头
MyArray<T>::MyArray(size_t count) {
	if (count) {
		data = new T[count]();
	}
	else {
		data = nullptr;
	}
}

template<typename T>  //记得先带上模版头
MyArray<T>::~MyArray() {
	if (data) {
		delete[] data;
	}
}

template<typename T>
typename MyArray<T>::iterator MyArray<T>::begin() const {
	return data;
}

template<typename T>
typename MyArray<T>::const_iterator MyArray<T>::cbegin() const {
	return data;
}
```

调用
```cpp
#include "标头.hpp"

int main() {
	MyArray<int> arrI(100);
	std::cout << *arrI.begin() << std::endl;
}
```

具体解释:
    ![[Pasted image 20250613161153.png]]
    ![[Pasted image 20250613161336.png]]
    ![[Pasted image 20250613161410.png]]
    ![[Pasted image 20250613161529.png]]

size_t是什么?
    ![[Pasted image 20250613161721.png]]
    (就是说在64为系统中size_t=unsigned long long)
    ![[Pasted image 20250613161814.png]]
    ![[Pasted image 20250613161825.png]]
    ![[Pasted image 20250613161840.png]]

为什么`typename MyArray<T>::iterator MyArray<T>::begin() const` , 最前面必须加typename ?
    ![[Pasted image 20250613163024.png]]
    ![[Pasted image 20250613163310.png]]

啥叫嵌套依赖类型? 非依赖类型?
    ![[Pasted image 20250613164545.png]]
    ![[Pasted image 20250613164610.png]]

`std::vector<int>::iterator it`  是非依赖的
    ![[Pasted image 20250613173934.png]]
