___
典型的普通函数模版
```cpp
namespace mystd
{
	template <typename iter_type, typename func_type>
	void for_each(iter_type first, iter_type last, func_type func) {
		for (auto iter = first; iter != last; ++iter) {
			func(*iter);
		}
	}
}

int main() {
	std::vector<int> ivec{ 1,2,3,4 };
//传进去的ivec.begin(),ivec.end()都是迭代器类型
	mystd::for_each(ivec.begin(), ivec.end(), [](int& elem) {
		++elem;
		});
	for (int elem : ivec) {
		std::cout << elem << std::endl;
	}
}
```
具体解释:
    ![[Pasted image 20250616164515.png]]
    ![[Pasted image 20250616164545.png]]
    ![[Pasted image 20250616164615.png]]

iter是迭代器类型,* iter就能取到值吗
    ![[Pasted image 20250616165844.png]]
    ![[Pasted image 20250616165904.png]]
    ![[Pasted image 20250616170000.png]]

上面其实用到了类型自动推测(就是推测iter_type和func_type的类型)
也可以显示写出来(一般不会写)
```cpp
namespace mystd
{
	template <typename iter_type, typename func_type>
	void for_each(iter_type first, iter_type last, func_type func) {
		for (auto iter = first; iter != last; ++iter) {
			func(*iter);
		}
	}
}

int main() {
	std::vector<int> ivec{ 1,2,3,4 };
	mystd::for_each <std::vector<int>::iterator,void(*)(int&)>
        	(ivec.begin(), ivec.end(), [](int& elem) {
		++elem;
		});
	for (int elem : ivec) {
		std::cout << elem << std::endl;
	}
}
```

___

成员函数模版
```cpp
template<typename T> //类模版头
class MyVector {
public:
	template<typename T2>  //成员函数模版头
	void outPut(const T2& elem) 
	/*{
		std::cout << elem << std::endl;
	}*/
};

//写在外面
template<typename T>
template<typename T2>
void MyVector<T>::outPut<T2>(const T2& elem) {  //这里的<T2>应该不写,不然报错
	std::cout << elem << std::endl;
}

int main(){
    MyVector<int> myVec;
    myVec.outPut<int>(20);
}
```

类模版头上面根本没用到,一定要写吗?
    ![[Pasted image 20250616172328.png]]
    ![[Pasted image 20250616172358.png]]
    所以, 只写T2,不写T会报错
    ![[Pasted image 20250616172534.png]]