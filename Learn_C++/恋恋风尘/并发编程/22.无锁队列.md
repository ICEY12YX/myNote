___
https://gitbookcpp.llfc.club/sections/cpp/concurrent/concpp22.html

队列只能在tail处push,head处pop
# 单一消费者和生产者队列

实现一个简单的无锁队列，只应对一个生产者一个消费者的情况
```cpp
#include<atomic>
#include<memory>

template<typename T>
class SinglePopPush
{
private:
    struct node{
        std::shared_ptr<T> data;
        node* next;
        node() : next(nullptr){}
    };
    
    std::atomic<node*> head;
    std::atomic<node*> tail;
    
    node* pop_head(){ //得到head节点
        node* const old_head = head.load();
        // ⇽-- - 1
        if (old_head == tail.load())   
        {
            return nullptr;
        }
        head.store(old_head->next);
        return old_head;
    }
public:
    SinglePopPush():head(new node), tail(head.load()){}
    SinglePopPush(const SinglePopPush& other) = delete;
    SinglePopPush& operator=(const SinglePopPush& other) = delete;
    ~SinglePopPush(){
        while (node* const old_head = head.load())
        {
            head.store(old_head->next);
            delete old_head;
        }
    }
    
    std::shared_ptr<T> pop(){
        node* old_head = pop_head();
        if (!old_head)
        {
            return std::shared_ptr<T>();
        }
        // ⇽-- -2
        std::shared_ptr<T> const res(old_head->data);  
        delete old_head;
        return res;
    }
    
    void push(T new_value){
        std::shared_ptr<T> new_data(std::make_shared<T>(new_value));
        // ⇽-- - 3
        node* p = new node;    
        //⇽-- - 4
        node* const old_tail = tail.load(); 
        //⇽-- - 5
        old_tail->data.swap(new_data);   
        //⇽-- - 6
        old_tail->next = p; 
        //⇽-- - 7
        tail.store(p);  //tail指向空节点  
    }
};
```
若多个线程并发调用push()或并发调用pop()，便会出问题
push中,如果有两个线程同时调用push()，就会分别构造一个新的空节点并分配内存3，而且都从tail指针读取相同的值4
结果它们都针对同一个尾节点更新其数据成员，却各自把data指针和next指针设置为不同的值5和6,这会造成混乱
pop_head()也有类似问题，若两个线程同时调用这个函数，它们就会读取同一个头节点而获得相同的next指针,就是old_head拿到一样的，而且都执行`head.store(old_head->next);`   
那两个线程都pop_head(),结果就pop了一次

___
# 多线程push

一种可行的方法是令pop()返回std::unique_ptr<>指针（凭此使之成为指涉目标对象的唯一指针），并在队列中存储指向数据的普通指针。
这样让代码得以按std::atomic的形式存储指针，支持必要的compare_exchange_strong()调用
```cpp
void push(T new_value)
{
    std::unique_ptr<T> new_data(new T(new_value));
    counted_node_ptr new_next;
    new_next.ptr=new node;
    new_next.external_count=1;
    
    for(;;)
    {
        //⇽--- 1
        node* const old_tail=tail.load();    
        T* old_data=nullptr;
        //⇽--- 2
        //不考虑别的线程情况的话
        //old_tail->data应该为空 old_data上面定义了也为空
        //两个相等,则old_tail->data=new_data的裸指针
        if(old_tail->data.compare_exchange_strong(old_data,new_data.get()))   
        {
            old_tail->next=new_next;
            // 3
            tail.store(new_next.ptr);    
            new_data.release();
            break;
        }
    }
}
```
为什么用unique_ptr
    ![[Pasted image 20250731224623.png]]
为什么用std::unique_ptr,不用std::shared_ptr
    ![[Pasted image 20250731234033.png]]
    总之就是这里临时用到一个std::unique_ptr,和队列整体设计无关

解释
    ![[Pasted image 20250731224749.png]]
    ![[Pasted image 20250731224815.png]]
    ![[Pasted image 20250731224827.png]]

为什么要用`release()`
    ![[Pasted image 20250731234530.png]]

但是上面的代码可能有问题
    ![[Pasted image 20250731235519.png]]
    ![[Pasted image 20250731235550.png]]
    ![[Pasted image 20250731235653.png]]


所以规范的操作是进行以下修改
(请注意，external_counters仅需使用两位，因为同一个节点最多只可能有两个外部计数器。因此，结构体count为它分配了一个两位的位域，而把internal_count设定为30位的整型值，从而维持了计数器32位的整体尺寸)
(这句可以完整看懂了再思考)
```cpp
template<typename T>
class lock_free_queue
{
private:
    struct node;
    struct counted_node_ptr 
    {
        int external_count;
        node* ptr;
    };
    std::atomic<counted_node_ptr> head;
    //⇽--- 1
    std::atomic<counted_node_ptr> tail;   
     
    struct node_counter
    {
        unsigned internal_count:30;  //这里的30,2是位数(可以不管)
        //⇽--- 2
        unsigned external_counters:2;   //主要的区别就是多了这个
    };
    struct node
    {
        std::atomic<T*> data;
        //⇽---  3
        std::atomic<node_counter> count;    
        counted_node_ptr next;  //next在这,有点绕
        
        node(){
            node_counter new_count;
            new_count.internal_count=0;
            //⇽---  4
            new_count.external_counters=2;    
            count.store(new_count);

            next.ptr=nullptr;
            next.external_count=0;
        }
    };
public:
	lock_free_queue() {
		counted_node_ptr new_next;
		new_next.ptr = new node();
		new_next.external_count = 1; //初始external_count为1
		tail.store(new_next);
		head.store(new_next);
		std::cout << "new_next.ptr is " << new_next.ptr << std::endl;
	}

	~lock_free_queue() {
		while (pop());
		auto head_counted_node = head.load();
		delete head_counted_node.ptr;
	}

    void push(T new_value){
        std::unique_ptr<T> new_data(new T(new_value));
        counted_node_ptr new_next;
        new_next.ptr = new node;
        new_next.external_count = 1;
        
        counted_node_ptr old_tail = tail.load();  //写在循环外了,而且存的是副本
        for(;;)
        {
            // 5
            //增加引用计数
            increase_external_count(tail,old_tail);     
            T* old_data=nullptr;
            // 6
            if(old_tail.ptr->data.compare_exchange_strong(   
               old_data,new_data.get()))
            {
                old_tail.ptr->next=new_next;
                old_tail=tail.exchange(new_next);
                //  7
                //减少old_tail 的引用计数（可能触发节点回收）
                free_external_counter(old_tail);    
                new_data.release();
                break;
            }
            //减少引用计数
            old_tail.ptr->release_ref();
        }
    }
};
```
为什么要把`counted_node_ptr old_tail = tail.load();`放在外面
    ![[Pasted image 20250801011611.png]]
    ![[Pasted image 20250801011651.png]]

上面我们写的是` old_tail=tail.exchange(new_next);`  上个版本的push写的是`tail.store(new_next.ptr);  ` 有什么区别
    ![[Pasted image 20250801002134.png]]
    ![[Pasted image 20250801010909.png]]
    主要原因就是后续函数中需要用到tail的最新值
    正常来说此时的old_tail应该就等于tail
    但是有可能别的线程改了tail(比如两个线程同时push)
    所以此时需要获得tail新值,然后tail到新的空节点上,然后把old_tail(就是刚刚tail新值)删了(或者延迟删)
    ![[Pasted image 20250801011442.png]]
    
___
# 多线程pop

和之前无锁栈类似
代码思路结构都相同,但是几处写了函数的地方内部操作会不一样
```cpp
template<typename T>
class lock_free_queue
{
public:
    std::unique_ptr<T> pop() //注意pop出来的数据(指针)我们用智能指针包裹
    {
        // 1
        //old_head是副本
        counted_node_ptr old_head=head.load(std::memory_order_relaxed);    
        for(;;)
        {
            //2
            //增加引用计数
            increase_external_count(head,old_head);  
              
            node* const ptr=old_head.ptr;
            if(ptr==tail.load().ptr) //头等于尾,说明队列为空
            {
                //3
                //减少引用计数
                ptr->release_ref();    
                return std::unique_ptr<T>(); 
            }
            // 4
            if(head.compare_exchange_strong(old_head,ptr->next))    
            {
                //res=ptr->data
                //并ptr->data=nullptr
                T* const res=ptr->data.exchange(nullptr);
                // 5
                //
                free_external_counter(old_head);   
                return std::unique_ptr<T>(res);
            }
            // 6
            //减少引用计数
            ptr->release_ref();    
        }
    }
};
```

减少引用计数和增加引用计数的思路大致相同(有些地方会有小不同)
大致思路
1
```cpp
        ref_node new_head;

        //2
        do {
            new_head = old_head;
            new_head._ref_count += 1;
        } while (!head.compare_exchange_weak(old_head, new_head));
        old_head = new_head; 
```
2
```cpp
//增加头部节点引用数量
void increase_head_count(counted_node_ptr& old_counter) {
    counted_node_ptr new_counter;

    do {
        new_counter = old_counter;
        ++new_counter.external_count;
    }//7  循环判断保证head和old_counter想等时做更新,多线程情况保证引用计数原子递增。
    while (!head.compare_exchange_strong(old_counter,  new_counter, 
        std::memory_order_acquire, std::memory_order_relaxed));
    //8  走到此处说明head的external_count已经被更新了
    old_counter.external_count = new_counter.external_count;
}
```
[[21.利用内存模型优化无锁栈]]

减少引用计数
减少的是internal_count
最后有个if判断较难理解
```cpp
template<typename T>
class lock_free_queue
{
private:
    struct node
    {
        void release_ref()
        {
            node_counter old_counter=
                count.load(std::memory_order_relaxed); //记录counter的副本
            //注意这个函数是写在struct node里面的
            //所以是针对每个node实例的
            //所以上面调用的时候是这样的ptr->release_ref();  (ptr是node类型)
            //所以上面自然能获得这个ptr实例中的计数器count

            node_counter new_counter;
            do
            {
                new_counter=old_counter;
                //1
                --new_counter.internal_count;    
            }
            //2
            while(!count.compare_exchange_strong(    
                  old_counter,new_counter,
                  std::memory_order_acquire,std::memory_order_relaxed));
            //本node实例的count与old_counter作比较,保证两者一样,中间没有线程竞争改了count,才能退出循环

            //这部分建议和下面一起看
            //内部计数internal_count为0,说明?
            //external_counters为0,说明?
            //两个都为0,表明调用release_ref()的是最后一个指涉目标节点的指针
            if(!new_counter.internal_count && !new_counter.external_counters)
            {
                //3
                delete this;    
            }
        }
    };
};
```

增加引用计数(和上面大致思路没区别)
<mark style="background: #FFF3A3A6;">增加的是external_count</mark>
```cpp
template<typename T>
class lock_free_queue
{
private:
    static void increase_external_count(
        std::atomic<counted_node_ptr>& counter,counted_node_ptr& old_counter)
    {
        counted_node_ptr new_counter;
        do
        {
            new_counter=old_counter;
            ++new_counter.external_count;
        }
        while(!counter.compare_exchange_strong(
              old_counter,new_counter,
              std::memory_order_acquire,std::memory_order_relaxed));
        old_counter.external_count=new_counter.external_count;
    }
};
```

对无锁队列的节点释放其外部计数器
```cpp
template<typename T>
class lock_free_queue
{
private:
    //在pop里传进来的是counted_node_ptr类型的old_head
    static void free_external_counter(counted_node_ptr &old_node_ptr)
    {
        node* const ptr=old_node_ptr.ptr;
        //增加的数量(就是除我以外,还有多少线程在引用old_head)
        int const count_increase=old_node_ptr.external_count-2;
        
        node_counter old_counter=ptr->count.load(std::memory_order_relaxed);
        node_counter new_counter;
        do
        {
            new_counter=old_counter;
            //⇽---  1
            --new_counter.external_counters;  
            //⇽---  2  
            new_counter.internal_count+=count_increase;    
        }
        //⇽---  3
        while(!ptr->count.compare_exchange_strong(    
              old_counter,new_counter,
              std::memory_order_acquire,std::memory_order_relaxed));
        if(!new_counter.internal_count &&
           !new_counter.external_counters)
        {
            //⇽---  4
            delete ptr;    
        }
    }
};
```
解释
    ![[Pasted image 20250801014632.png]]
    ![[Pasted image 20250801014832.png]]
    ![[Pasted image 20250801022044.png]]

函数解释
    ![[Pasted image 20250801024018.png]]
    这个我们都知道
    ![[Pasted image 20250801024250.png]]
    ![[Pasted image 20250801025130.png]]
    ![[Pasted image 20250801025502.png]]
    ![[Pasted image 20250801025826.png]]
    看了下面的解释,再来看
    线程1,2,3同时pop
    如果线程2还在引用节点,还没释放记一笔
    那internal_count比如=-1  然后cout_increase=2  
     (线程2,3,在引用这个节点,线程3释放记一笔了)
     (现在处在线程1)
    那internal_count+=cout_increase  internal_count就是1  (这个1代表还有线程在引用)
    不等于0,所以不能删
    然后就可能发生上面这个例子
    (上面那个例子应该发生在线程3身上,但是很奇怪,照理说节点被pop出去,线程3应该到不了free_external_counter这一步,先别管了)
    (所以应该internal_count可以为正数或负数,就是上面的例子不一定发生)
    之后照理来说,线程2发现pop不了,就会进入release_ref
    然后`--new_counter.internal_count;  `  
    internal_count就是0了!!!!!!!!!!!!!!!!!!!!!
    就可以删节点了(所以release_ref也有删节点的逻辑)

关于internal_count还是不懂
    ![[Pasted image 20250801030523.png]]
    上面pop/push那里说会增加internal_count(别管,ai错了)
    ![[Pasted image 20250801030939.png]]

所以现在就可以理解为什么internal_count=0并且external_counters=0的时候,可以删除节点了

完整代码
```cpp
template<typename T>
class lock_free_queue
{
private:

	struct node_counter
	{
		unsigned internal_count : 30;
		//⇽--- 2
		unsigned external_counters : 2;
	};

	struct node;

	struct counted_node_ptr
	{
		//构造初始化各成员
		counted_node_ptr() :external_count(0), ptr(nullptr) {}
		int external_count;
		node* ptr;
	};

	struct node
	{
		std::atomic<T*> data;
		std::atomic<node_counter> count;
		counted_node_ptr next; next;

		node() {
			node_counter new_count;
			new_count.internal_count = 0;
			//⇽---  4
			new_count.external_counters = 2;
			count.store(new_count);

			next.ptr = nullptr;
			next.external_count = 0;
		}


		void release_ref()
		{
			std::cout << "call release ref " << std::endl;
			node_counter old_counter = count.load(std::memory_order_relaxed);
			node_counter new_counter;
			do
			{
				new_counter = old_counter;
				//1
				--new_counter.internal_count;
			}
			//2
			while (!count.compare_exchange_strong(
				old_counter, new_counter,
				std::memory_order_acquire, std::memory_order_relaxed));
			if (!new_counter.internal_count &&
				!new_counter.external_counters)
			{
				//3
				delete this;
			}
		}
	};

	std::atomic<counted_node_ptr> head;
	std::atomic<counted_node_ptr> tail;

	static void free_external_counter(counted_node_ptr& old_node_ptr)
	{
		std::cout << "call  free_external_counter " << std::endl;
		node* const ptr = old_node_ptr.ptr;
		int const count_increase = old_node_ptr.external_count - 2;
		node_counter old_counter =
			ptr->count.load(std::memory_order_relaxed);
		node_counter new_counter;
		do
		{
			new_counter = old_counter;
			//⇽---  1
			--new_counter.external_counters;
			//⇽---  2  
			new_counter.internal_count += count_increase;
		}
		//⇽---  3
		while (!ptr->count.compare_exchange_strong(
			old_counter, new_counter,
			std::memory_order_acquire, std::memory_order_relaxed));
		if (!new_counter.internal_count &&
			!new_counter.external_counters)
		{
			//⇽---  4
			delete ptr;
		}

	}


	static void increase_external_count(
		std::atomic<counted_node_ptr>& counter, counted_node_ptr& old_counter)
	{
		counted_node_ptr new_counter;
		do
		{
			new_counter = old_counter;
			++new_counter.external_count;
		} while (!counter.compare_exchange_strong(
			old_counter, new_counter,
			std::memory_order_acquire, std::memory_order_relaxed));
		old_counter.external_count = new_counter.external_count;
	}

public:
	lock_free_queue() {

		counted_node_ptr new_next;
		new_next.ptr = new node();
		new_next.external_count = 1;
		tail.store(new_next);
		head.store(new_next);
		std::cout << "new_next.ptr is " << new_next.ptr << std::endl;
	}

	~lock_free_queue() {
		while (pop());
		auto head_counted_node = head.load();
		delete head_counted_node.ptr;
	}

	void push(T new_value) {
		std::unique_ptr<T> new_data(new T(new_value));
		counted_node_ptr new_next;
		new_next.ptr = new node;
		new_next.external_count = 1;

		counted_node_ptr old_tail = tail.load();  //写在循环外了,而且存的是副本
		for (;;)
		{
			// 5
			//增加引用计数
			increase_external_count(tail, old_tail);
			T* old_data = nullptr;
			// 6
			if (old_tail.ptr->data.compare_exchange_strong(
				old_data, new_data.get()))
			{
				old_tail.ptr->next = new_next;
				old_tail = tail.exchange(new_next);
				//  7
				//减少old_tail 的引用计数（可能触发节点回收）
				free_external_counter(old_tail);
				new_data.release();
				break;
			}
			//减少引用计数
			old_tail.ptr->release_ref();
		}
	}


	std::unique_ptr<T> pop()
	{
		counted_node_ptr old_head = head.load(std::memory_order_relaxed);
		for (;;)
		{
			increase_external_count(head, old_head);
			node* const ptr = old_head.ptr;
			if (ptr == tail.load().ptr)
			{
				//头尾相等说明队列为空，要减少内部引用计数
				ptr->release_ref();
				return std::unique_ptr<T>();
			}
			//  ⇽---  2
			counted_node_ptr next = ptr->next.load();
			if (head.compare_exchange_strong(old_head, next))
			{
				T* const res = ptr->data.exchange(nullptr);
				free_external_counter(old_head);
				return std::unique_ptr<T>(res);
			}
			ptr->release_ref();
		}
	}
};

```

___
