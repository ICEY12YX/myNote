___
https://gitbookcpp.llfc.club/sections/cpp/concurrent/concpp08.html
# 快速排序

基本步骤：
1. 选择一个基准元素（pivot）：从数组中选择一个元素作为基准元素。选择基准元素的方式有很多种，常见的是选择数组的第一个元素或最后一个元素。
2. 分区（partitioning）：重新排列数组，把比基准元素小的元素放在它的左边，把比基准元素大的元素放在它的右边。在这个过程结束时，基准元素就处于数组的最终位置。
3. 递归排序子数组：递归地对基准元素左边和右边的两个子数组进行快速排序。

```cpp
//c++ 版本的快速排序算法
template<typename T>
void quick_sort_recursive(T arr[], int start, int end) {
    if (start >= end) return;
    T key = arr[start];
    int left = start, right = end;
    while(left < right) {
        while (arr[right] >= key && left < right) right--;  //找一个比key小的在右边的
        while (arr[left] <= key && left < right) left++;  //找一个比key大的在左边的
        std::swap(arr[left], arr[right]);  //交换
    }

    //这里要记一下
    if (arr[left] < key) {
        std::swap(arr[left], arr[start]);
    }

    //递归调用快排
    quick_sort_recursive(arr, start, left - 1);
    quick_sort_recursive(arr, left + 1, end);
}

template<typename T>
void quick_sort(T arr[], int len) {
    quick_sort_recursive(arr, 0, len - 1);
}

//调用
void test_quick_sort() {
    int num_arr[] = { 5，3，7，6，4，1，0，2，9，10，8 };
    int length = sizeof(num_arr) / sizeof(int);
    quick_sort(num_arr, length );
    std::cout << "sorted result is ";
    for (int i = 0; i < length; i++) {
        std::cout << " " << num_arr[i];
    }

    std::cout << std::endl;    
}
```
但是C++已经帮我们快速排序封装好了,使用了函数式编程

___
# 函数式编程

函数式编程是一种编程范式
    ![[Pasted image 20250711160951.png]]
    ![[Pasted image 20250711161032.png]]

我们把上面的快速排序改成函数式编程
```cpp
template<typename T>
std::list<T> sequential_quick_sort(std::list<T> input)  //假设传进来的是一个list,同时返回一个新的list
{
    if (input.empty())  //判断list是否为空
    {
        return input;  //空就直接返回
    }
    std::list<T> result;

    //  ① 将input中的第一个元素放入result中，并且将这第一个元素从input中删除
    result.splice(result.begin(), input, input.begin());  

    //  ② 取result的第一个元素，将来用这个元素做哨兵元素。
    T const& pivot = *result.begin();    

    // ③ std::partition 是一个标准库函数，用于将容器或数组中的元素按照指定的条件进行分区，
    // 使得满足条件的元素排在不满足条件的元素之前。
    // 所以经过计算divide_point指向的是input中第一个大于等于pivot的元素
    auto divide_point = std::partition(input.begin(), input.end(),[&](T const& t) {return t < pivot; });   //每个元素就是t,依次把每个元素和pivot作比较 

    // ④ 我们将小于pivot的元素放入lower_part中
    //divide_point正好是个指针,也算是一种迭代器
    std::list<T> lower_part;
    lower_part.splice(lower_part.end(), input, input.begin(), divide_point); 

    //那input里剩下的就是比pivot大的(或等于)元素了

    // ⑤ 递归(让lower_part变的有序)
    auto new_lower(sequential_quick_sort(std::move(lower_part)));    
    // ⑥ 递归(让input变的有序)
    auto new_higher(sequential_quick_sort(std::move(input)));  
      
    //⑦ 到此时new_higher和new_lower都是从小到大排序好的列表
    //将new_higher 拼接到result的尾部,将new_lower 拼接到result的头部
    result.splice(result.end(), new_higher);    
    result.splice(result.begin(), new_lower);   
    return result;
}

void test_sequential_quick() {
    std::list<int> numlists = { 6,1,0,7,5,2,9,-1 };
    auto sort_result = sequential_quick_sort(numlists);
    std::cout << "sorted result is ";
    for (auto iter = sort_result.begin(); iter != sort_result.end(); iter++) {
        std::cout << " " << (*iter);
    }
    std::cout << std::endl;
}
```

`result.splice(result.begin(), input, input.begin()); `
    ![[Pasted image 20250711161424.png]]
    ![[Pasted image 20250711161435.png]]
    ![[Pasted image 20250711161445.png]]
    所以上面代码的意思就是
    将input中的第一个元素放入result中，并且将这第一个元素从input中删除

为什么递归的时候都是用的移动赋值?
    ![[Pasted image 20250711171654.png]]

___
# 并行方式

但是上面那种代码是串行的
我们想写成并行的(递归的时候可以多线程并行)
```cpp
//并行版本
template<typename T>
std::list<T> parallel_quick_sort(std::list<T> input)
{
    if (input.empty())
    {
        return input;
    }
    std::list<T> result;
    
    result.splice(result.begin(), input, input.begin());
    T const& pivot = *result.begin();
    
    auto divide_point = std::partition(input.begin(), input.end(),
        [&](T const& t) {return t < pivot; });
        
    std::list<T> lower_part;
    lower_part.splice(lower_part.end(), input, input.begin(),divide_point);
    //截止这里,和上面代码都一样

    // ①因为lower_part和input不得噶了，
    // 所以操作lower_part的时候不会影响input，
    // 所以这里可以启动线程,返回future
    std::future<std::list<T>> new_lower(
        std::async(&parallel_quick_sort<T>, std::move(lower_part)));
    //(这里可能因为是模版,传函数的时候要加&和<T>)
    //记得用移动赋值std::move,节省性能
    //否则就是复制一份过去了

    // 剩下的input还是串行的方式排序
    auto new_higher(parallel_quick_sort(std::move(input)));  
    
    result.splice(result.end(), new_higher);    
    result.splice(result.begin(), new_lower.get());    
    return result;  //这个在线程中就会作为future的返回值
}
```

函数式编程的好处(大致看看)
    ![[Pasted image 20250711172047.png]]

___
# ThreadPool方式的并行排序

但是上面我们理论上实现的并行,要是数组元素多,那线程会随着不断递归启的越来越多
虽然async在线程多的时候,会自己通过计算,选择串行的方式接着运行

然后async好处是,不用手动管理线程了(不用自己写join,detach),他自己内部有一套逻辑
但是坏处就是,自己没办法控制线程了

所以可以用线程池的方式,将快排的任务传进去
```cpp
//线程池版本
//并行版本
template<typename T>
std::list<T> thread_pool_quick_sort(std::list<T> input)
{
    if (input.empty())
    {
        return input;
    }
    std::list<T> result;
    
    result.splice(result.begin(), input, input.begin());
    T const& pivot = *result.begin();
    
    auto divide_point = std::partition(input.begin(), input.end(),
        [&](T const& t) {return t < pivot; });
        
    std::list<T> lower_part;
    lower_part.splice(lower_part.end(), input, input.begin(),divide_point);

    //上面是用async启动线程,这里,是把任务交给线程池
    // 这里投递给线程池处理
    auto new_lower = ThreadPool::instance().commit(&thread_pool_quick_sort<T>, std::move(lower_part));
    
    auto new_higher(thread_pool_quick_sort(std::move(input)));
    
    result.splice(result.end(), new_higher);
    result.splice(result.begin(), new_lower.get());
    return result;
}

void test_thread_pool_sort() {
    std::list<int> numlists = { 6,1,0,7,5,2,9,-1 };
    auto sort_result = thread_pool_quick_sort(numlists);
    std::cout << "sorted result is ";
    for (auto iter = sort_result.begin(); iter != sort_result.end(); iter++) {
        std::cout << " " << (*iter);
    }
    std::cout << std::endl;
}
```