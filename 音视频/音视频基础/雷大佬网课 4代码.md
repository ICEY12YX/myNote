___
# 解释
## part1
```cpp
av_register_all();
avformat_network_init();
pFormatCtx = avformat_alloc_context();

//打开文件+打开流
if (avformat_open_input(&pFormatCtx, filepath, NULL, NULL) != 0) {
	printf("Couldn't open input stream.\n");
	return -1;
}
if (avformat_find_stream_info(pFormatCtx, NULL) < 0) {
	printf("Couldn't find stream information.\n");
	return -1;
}
```

![[Pasted image 20250529191255.png]]

![[Pasted image 20250529192654.png]]
![[Pasted image 20250529193102.png]]

![[Pasted image 20250529192957.png]]
![[Pasted image 20250529193120.png]]

## part2
```cpp
//从AVFormatContext里操作AVstreams
videoindex = -1;
for (i = 0; i < pFormatCtx->nb_streams; i++) {  
	if (pFormatCtx->streams[i]->codec->codec_type == AVMEDIA_TYPE_VIDEO) {
		videoindex = i;
		break;
	}
}
if (videoindex == -1) {
	printf("Didn't find a video stream.\n");
	return -1;
}
```

`videoindex`记录找到的视频流位置
![[Pasted image 20250529194510.png]]
pFormatCtx->streams[i] 就是AVStream类型
![[Pasted image 20250529193455.png]]
pFormatCtx->streams[i]->codec就是AVCodecContext类型,然后里面有codec_type参数
![[Pasted image 20250529194703.png]]
![[Pasted image 20250529195118.png]]
![[Pasted image 20250529195141.png]]


```cpp
pCodecCtx = pFormatCtx->streams[videoindex]->codec;

//获取解码器
pCodec = avcodec_find_decoder(pCodecCtx->codec_id);
if (pCodec == NULL) {
	printf("Codec not found.\n");
	return -1;
}

//打开解码器
if (avcodec_open2(pCodecCtx, pCodec, NULL) < 0) {
	printf("Could not open codec.\n");
	return -1;
}
```

pFormatCtx->streams[i]->codec就是AVCodecContext类型
![[Pasted image 20250529195031.png]]

![[Pasted image 20250529195018.png]]
![[Pasted image 20250529195247.png]]
![[Pasted image 20250529195236.png]]
___
## part3
```cpp
pFrame = av_frame_alloc();
pFrameYUV = av_frame_alloc();
```
![[Pasted image 20250529215737.png]]
注意这两个都是放解码后的数据,一个是放初始解码后的一帧数据,一个是放格式化后的一帧数据


```cpp
out_buffer = (uint8_t*)av_malloc(
	avpicture_get_size(PIX_FMT_YUV420P, pCodecCtx->width, pCodecCtx->height)
);
```
![[Pasted image 20250529215819.png]]

avpicture_get_size的定义
![[Pasted image 20250529215858.png]]
其中AVPixelFormat 可以取的值长这样
![[Pasted image 20250529215934.png]]


```cpp
avpicture_fill((AVPicture*)pFrameYUV, out_buffer, 
	PIX_FMT_YUV420P, pCodecCtx->width, pCodecCtx->height);
```
![[Pasted image 20250529220809.png]]

数据之后会被存到out_buffer里,那数据在这个空间里具体长什么样?
长这样
![[Pasted image 20250529222108.png]]

pFrameYUV是AVFrame类型
里面的data是这么定义的
这个不是一般的数组,  这是一个包含了8个指针的数组
这样是为了像下面那样可以保存不同分量的起始地址
![[Pasted image 20250529223812.png]]

里面的linesize就是普通数组 表示行跨度（字节数）
![[Pasted image 20250529224130.png]]

![[Pasted image 20250529223439.png]]
这样就可以关联上out_buffer和pFrameYUV
![[Pasted image 20250529230305.png]]


avpicture_fill定义
![[Pasted image 20250529220140.png]]
AVPicture是个结构体
![[Pasted image 20250529224550.png]]
![[Pasted image 20250529224635.png]]
所以上面可以看到AVPicture其实就是把几个特别有用的成员单独拉出来,搞了这么个结构

![[Pasted image 20250529224754.png]]


```cpp
packet = (AVPacket*)av_malloc(sizeof(AVPacket));
```
分配一个压缩数据包容器，用于从文件读取原始数据
(一会用来放从文件里读出的每一帧压缩数据)


```cpp
printf("--------------- File Information ----------------\n");
av_dump_format(pFormatCtx, 0, filepath, 0);
printf("-------------------------------------------------\n");
```
函数定义
![[Pasted image 20250529225654.png]]
![[Pasted image 20250529225623.png]]打印结果
![[Pasted image 20250529225147.png]]


```cpp
img_convert_ctx = sws_getContext(pCodecCtx->width, 
	pCodecCtx->height, pCodecCtx->pix_fmt,
	pCodecCtx->width, pCodecCtx->height,
	PIX_FMT_YUV420P, SWS_BICUBIC, NULL, NULL, NULL);
```
是在初始化一个**图像缩放和格式转换的上下文**

![[Pasted image 20250529230546.png]]
和上面的参数意义对照
![[Pasted image 20250529230740.png]]
其中pCodecCtx->pix_fmt我们可以找到,pix_fmt就是AVPixelFormat类型
![[Pasted image 20250529230910.png]]

![[Pasted image 20250529230800.png]]
![[Pasted image 20250529231000.png]]

关于那个算法,大概意思是每个数字代表一种算法,然后这些数字已经被define成代号了,写在这里
![[Pasted image 20250529225828.png]]

返回的是一个结构体struct SwsContext*
它是 FFmpeg 中专门负责**图像缩放和像素格式转换的核心处理器**
里面大概长这样
![[Pasted image 20250529232328.png]]

然后回到函数处理操作本身
![[Pasted image 20250529231940.png]]
![[Pasted image 20250529232025.png]]
___
## part4

```cpp
	frame_cnt = 0;  //准备计数器，记录已解码的帧数
	while (av_read_frame(pFormatCtx, packet) >= 0) {
		if (packet->stream_index == videoindex) {
			/*
			 * 在此处添加输出H264码流的代码
			 * 取自于packet，使用fwrite()
			 */

			 //解码
			ret = avcodec_decode_video2(pCodecCtx, pFrame, &got_picture, packet);
			if (ret < 0) {
				printf("Decode Error.\n");
				return -1;
			}
			if (got_picture) {
				sws_scale(img_convert_ctx, 
				    (const uint8_t* const*)pFrame->data, pFrame->linesize, 
					0, pCodecCtx->height,
					pFrameYUV->data, pFrameYUV->linesize
				);
				printf("Decoded frame index: %d\n", frame_cnt);

				/*
				 * 在此处添加输出YUV的代码
				 * 取自于pFrameYUV，使用fwrite()
				 */

				frame_cnt++;

			}
		}
		av_free_packet(packet);
	}
```


![[Pasted image 20250529233232.png]]
![[Pasted image 20250529233600.png]]

![[Pasted image 20250529233625.png]]

解码函数avcodec_decode_video2
![[Pasted image 20250529233757.png]]
![[Pasted image 20250529234718.png]]
got_Frame是int类型,我们是地址传进去的所以相当于返回值,它要么0,要么1,1就代表成功解码出一帧
解码出来的pFrame格式是不确定的
![[Pasted image 20250529234745.png]]

sws_scale函数定义  是用来将解码后的任意格式 转为 我们想要的格式,也可以进行缩放
其中第一个传进去的是SwsContext结构体,就是我们sws_getContext函数中返回的.
在sws_getContext函数中我们定义了想要图像转为什么格式,要不要拉伸
这里sws_scale函数我们就把这个"模版"放进来
![[Pasted image 20250529234809.png]]

前面两个参数srcSlice和srcStride就分别放源数据pFrame->data和pFrame->linesize
最后两个参数也同理
![[Pasted image 20250530002222.png]]
___
# bug
## bug1
 char filepath[] = "Titanic.ts"; 路径问题
把Titanic.ts直接放在我的src文件夹里,就是和cpp文件放一起,用这个相对路径是不对的
这个相对路径指向的是F:\University\Audio-and-video\coding\Project2\Project2 这里
![[Pasted image 20250529185436.png]]
如果想把Titanic.ts放进input里面那路径要写成"input/Titanic.ts"
___
